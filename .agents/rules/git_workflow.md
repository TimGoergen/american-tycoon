# Git Workflow Rule

Always commit every completed body of work into a dedicated feature branch as soon as it is finished, and push that branch to origin remote (`git push -u origin <branch-name>`). Never leave completed work uncommitted on `main`.
